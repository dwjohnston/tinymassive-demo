import React, {
    Component,
} from 'react';
import { withStyles } from '@material-ui/core/styles';

class SocketView extends Component {
    constructor(props) {
        super(props);
        this.state = {};
    }
    render() {
        return <main>

        </main>;
    }
}

const styles = {
    root: {},
};
export default withStyles(styles)(SocketView);
